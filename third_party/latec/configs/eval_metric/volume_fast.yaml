

# ------------------------------
# Faithfulness Metrics
# ------------------------------
faithfulnesscorrelation:
  nr_runs: 15              # 100 ? 15 (reduce perturbations)
  subset_size: 2304         # 2304 ? 512 (random voxel subset)
  perturb_baseline: "black"
  softmax: False

faithfulnessestimate:
  features_in_step: 4608    # 2304 ? 512
  perturb_baseline: "black"
  softmax: False

monotonicitycorrelation:
  nr_samples: 2            # 10 ? 2
  features_in_step: 4608
  perturb_baseline: "uniform"
  softmax: False

pixelflipping:
  features_in_step: 9216
  perturb_baseline: "black"
  softmax: False

regionperturbation:
  patch_size: 24           # 24 ? 16 (smaller patches)
  regions_evaluation: 2    # 20 ? 2 (fewer regions)
  perturb_baseline: "uniform"
  softmax: False

insertion:
  pixel_batch_size: 55296    # 2304 ? 512
  sigma: 0.1
  kernel_size: 1
  softmax: False

deletion:
  pixel_batch_size:  55296   
  softmax: False

irof:
  segmentation_method: "slic"
  perturb_baseline: "black"
  softmax: False

road:
  noise: 0.1
  percentages_max: 50      # 100 ? 50% to halve iterations
  softmax: False

sufficiency:
  threshold: 0.75
  softmax: False

infidelity:
  n_perturb_samples: 15    # 50 ? 15 (random noise samples)
  softmax: False


# ------------------------------
# Robustness Metrics
# ------------------------------
locallipschitzestimate:
  nr_samples: 2
  perturb_std: 0.1
  perturb_mean: 0.0
  softmax: False

maxsensitivity:
  nr_samples: 2
  lower_bound: 0.1
  softmax: False

continuity:
  patch_size: 16
  nr_steps: 10
  perturb_baseline: "uniform"
  softmax: False

relativeinputstability:
  nr_samples: 3
  softmax: False

relativeoutputstability:
  nr_samples: 3
  softmax: False

relativerepresentationstability:
  nr_samples: 3
  softmax: False


# ------------------------------
# Complexity Metrics
# ------------------------------
sparseness:
  softmax: False

complexity:
  softmax: False

effectivecomplexity:
  eps: 0.001
  softmax: False
